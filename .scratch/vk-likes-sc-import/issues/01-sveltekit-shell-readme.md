# 01 — SvelteKit + Tailwind shell + friend README

**What to build:** A local app boots on localhost with a modern Tailwind shell and clear empty states (not connected, no library). A short README lets a friend install Node, start the app, and open the browser.

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [x] SvelteKit + TypeScript + Tailwind project runs locally without React
- [x] Landing/empty states explain next steps (connect SoundCloud, load Source Library)
- [x] README covers install → start → open browser for a non-author user
- [x] Out of scope called out: no FreeYourMusic MCP, no file re-upload
